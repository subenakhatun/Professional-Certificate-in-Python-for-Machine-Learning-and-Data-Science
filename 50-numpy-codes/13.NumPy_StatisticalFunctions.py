# NumPy - Statistical Functions: np.mean(arr)/median(arr)/mode(arr)/std(arr)/var(arr)
import numpy as np

# Creating an array
array_1 = np.array([1, 2, 3, 4, 5])
print(f'Mean of array: {np.mean(array_1)}')